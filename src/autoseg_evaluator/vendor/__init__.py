"""Third-party numerical kernels, vendored byte-identical. Do not edit.

Everything below this package is supplied code, reproduced exactly as it was
delivered. Its acceptance thresholds run to 1e-10 mm, and reformatting code
that dense — even to match house style — is how a silent numerical change
happens. ``tests/test_vendor_integrity.py`` asserts every file here still
matches the supplier's own SHA-256 manifest, so an accidental edit fails the
suite rather than quietly moving a published result.

Changes belong upstream. If something here has to change, it changes in the
supplier's package, comes back with a new manifest, and the pinned hashes move
with it.

See ``third_party/native_contour_metrics/`` for the manifests, the C++ source,
the build and validation scripts, and the licences, and
``docs/V3_POLYGON_METRICS_SPEC.md`` for what is integrated and why.
"""

from __future__ import annotations

__all__: list[str] = []
