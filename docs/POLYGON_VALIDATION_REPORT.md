# Native polygon metrics — validation report

Generated by `scripts/validate_polygon_metrics.py`. Regenerate it rather
than editing it.

**PASSED** · 2026-09-22 03:13 UTC

## What was checked

| Suite | Result | Pairs | Seconds | What it proves |
|---|---|---:|---:|---|
| `public` | pass | 150 | 32.7 | the vendored kernel reproduces the published values |
| `stress` | pass | 44 | 2.3 | geometric edge cases behave, including refusals that must stay refusals |
| `adapter (fast)` | pass | 150 | 33.2 | this application's call path preserves them, directions included |
| `adapter (reference)` | pass | 150 | 543.5 | this application's call path preserves them, directions included |
| `dicom` | pass | 150 | 109.5 | our grid builder and parser reach them from the DICOM files themselves |

## Largest disagreement with the published values

### `adapter (fast)` — fast 0.2.0.dev2

| Quantity | Largest error | Threshold |
|---|---:|---:|
| Symmetric distances | 4.856e-10 mm | 0.001 mm |
| APL, both directions | 2.619e-10 mm | 1e-08 mm |
| NAPL, both directions | 1.665e-15 | 1e-10 |

Measured under `engine=fast`, `quantile_guard_mm=0.001`, `version=0.2.0.dev2`

### `adapter (reference)` — reference 0.1.0

| Quantity | Largest error | Threshold |
|---|---:|---:|
| Symmetric distances | 7.062e-04 mm | 0.001 mm |
| APL, both directions | 2.910e-11 mm | 1e-08 mm |
| NAPL, both directions | 3.331e-16 | 1e-10 |

Measured under `engine=reference`, `sampling_mm=0.001`, `version=0.1.0`

### `dicom` — fast 0.2.0.dev2

| Quantity | Largest error | Threshold |
|---|---:|---:|
| Symmetric distances | 4.856e-10 mm | 0.001 mm |
| APL, both directions | 2.619e-10 mm | 1e-08 mm |
| NAPL, both directions | 1.665e-15 | 1e-10 |

Measured under `engine=fast`, `quantile_guard_mm=0.001`, `version=0.2.0.dev2`

## Settings

- Tolerances: 1 mm, 2 mm
- Missing-plane policy: `exclude`, the convention the published study uses

Per-suite settings are stated with each result above, because they differ.
The reference engine is validated at the sampling step its own published
acceptance was recorded at (0.001 mm), not the
coarser step it runs at in the application (0.02 mm). At
the coarser step it disagrees with the audited continuous values by up to
7e-3 mm — inside its own stated interval, outside the suppliers' threshold.
Judging it there would measure the sampling step and call it a defect.

The thresholds above are the suppliers', not ours.

## What this does not cover

These are synthetic shapes with regular slice spacing and clean topology.
They say nothing about vendor structure sets with nested rings, irregular
stacks or empty ROIs; those are covered by `tests/test_contour_grid.py`
and `tests/test_polygon_metrics.py`, against cases taken from real data.
